class Children:
    # The number of Children is 3.
    # We need to distribute candies so that no children gets more than the limit.
    def distributeCandies(self, n: int, limit: int) -> int:
        pass



if __name__ == '__main__':
    children = Children()
    n = 5
    limit = 2
    print(children.distributeCandies(n, limit))
